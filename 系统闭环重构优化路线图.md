# 系统闭环重构优化路线图

## 目标

本路线图用于指导后续重构与优化。核心目标不是继续堆功能或单纯调数值，而是把已经存在的经济、军事、外交、内容、界面与测试系统整理成可验证、可维护、可长期运行的闭环。

所有新增内容、修改说明、测试名称、注释与文档要求优先使用中文。除非必须对接已有英文标识、外部库、编译器约定或现有代码命名，否则新文档、新界面文案、新测试说明都应使用中文。

## 总体判断

当前项目已有大量系统入口和局部实现，但主要问题是系统之间缺少统一权威源与真实结算闭环。

主要症状包括：

- 经济市场有供需、价格、库存、满意度，但缺少真实成交清算。
- 财政有收入、支出、MEFO、投资池，但研究、建造、军购、融资没有统一资金账户。
- 军事 AI 有前线、登陆、海军支援、战区策略，但缺少战役计划闭环。
- 外交、事件、focus、decision、situation 已有运行时，但 GUI、headless、schedule 路径不统一。
- UI 面板大量重新估算逻辑数据，可能与真实逻辑字段脱节。
- 测试多为稳定性和函数级验证，缺少长期回放、守恒、历史节奏、GUI/headless 一致性验证。

## 全局重构原则

1. 先修闭环，再调数值。
2. 先统一运行时，再补历史内容。
3. 先建立权威数据源，再优化 UI 展示。
4. 所有重大系统必须有守恒测试、长期回放测试或历史行为测试。
5. AI 只输出意图，底层执行层统一写真实状态。
6. UI 只展示逻辑层快照，不在渲染路径里重新推导核心规则。
7. 任何自动融资、自动参战、自动和平、自动 focus 完成等隐式机制必须有可见日志或测试。

## 按优先级拆解的修复任务清单

本节是后续动手修复时的主任务表。每个任务都写明关联文件、问题、修改方向和验收标准。

### P0.1：统一 content runtime，修复 GUI/headless 不一致和高速跳日 ✅ 已完成

关联文件：

- `crates/hoi4-app/src/update_loop.rs`
- `crates/hoi4-app/src/bootstrap.rs`
- `crates/hoi4-runtime/src/runtime.rs`
- `crates/hoi4-runtime/src/content_runtime.rs`
- `crates/hoi4-runtime/src/schedule.rs`
- `crates/hoi4-runtime/src/lib.rs`
- `crates/hoi4-integration/src/lib.rs`

问题：

- GUI 路径在 app update loop 中额外调用 content daily。
- headless 路径只调用 `tick_one_hour`，不跑完整 content runtime。
- 一帧推进多天时，content daily 只跑一次，中间日期丢失。

修复方向：

- 新增统一的 `ContentRuntimeState` 或同等结构，集中保存 focus、event、decision、situation、global flags、scripted surrender、last content day。
- 将 `tick_content_day_change` 从 app 层移动到 runtime day-change 流程，或由 `tick_one_hour` 返回的每个 `DayChanged` 事件逐日驱动。
- headless 初始化必须构造同一套 content state。
- `update_loop.rs` 不应拥有一套只有 GUI 才执行的历史内容逻辑。

验收标准：

- ✅ GUI 与 headless 推进同样天数后，focus、event、decision、situation 结果一致。
- ✅ 一次推进 72 小时，content daily 执行 3 次。
- 1937 年卢沟桥 flag 在 GUI 与 headless 都能触发。

### P0.2：挂接法律冷却与法律生效流程 ✅ 已完成

关联文件：

- `crates/hoi4-content/src/v6_loader.rs`
- `crates/hoi4-runtime/src/schedule.rs`
- `crates/hoi4-app/src/main.rs`
- `crates/hoi4-ui/src/law_panel.rs`
- `crates/hoi4-state/src/laws.rs`

问题：

- `set_law` 会扣 PP 并设置 pending/cooldown。
- `tick_law_cooldowns` 存在，但没有挂进日更调度。
- 玩家点法律后可能永远 pending，不生效。

修复方向：

- 在 `schedule.rs` 中新增法律日更入口，或并入 politics daily。
- 明确顺序：法律冷却推进、法律生效、副作用执行、modifier 重算、经济 tick。
- UI 命令不能静默吞掉 `set_law` 错误，应返回中文提示或日志。

验收标准：

- 调用 `set_law` 后推进足够天数，`slot.current` 变为目标法律。
- PP 不足、冷却未完成、条件不满足时有错误报告。
- 经济法变化能影响当日或次日经济 tick。

### P0.3：统一 focus 权威运行时，禁止双推进 ✅ 已完成

关联文件：

- `crates/hoi4-logic/src/politics.rs`
- `crates/hoi4-content/src/focus_tick.rs`
- `crates/hoi4-content/src/focus.rs`
- `crates/hoi4-ui/src/focus_tree_panel.rs`
- `crates/hoi4-runtime/src/schedule.rs`
- `crates/hoi4-app/src/main.rs`

问题：

- politics daily 与 RON content focus daily 同时可能推进 `current_focus/focus_progress`。
- UI 使用 RON focus，但后台可能由 vanilla politics focus 处理同一字段。
- focus `available` 条件未被 UI 和 start 逻辑执行。

修复方向：

- 明确 RON focus 或 vanilla focus 为唯一权威。
- 若 RON 为权威，从 `politics_daily` 移除 focus progress 推进，仅保留政治点数和 modifier。
- `start_focus` 必须检查 `available` trigger。
- UI focus 状态必须展示 `available` 条件失败原因。
- focus completion effect 只允许一个 runner 执行。

验收标准：

- 70 天 focus 推进 70 天只完成一次。
- `available = false` 的 focus 无法开始，UI 显示锁定。
- focus 完成效果只执行一次。

### P0.4：建立商品市场统一清算层 ✅ 已完成

关联文件：

- `crates/hoi4-logic/src/economy/market_tick.rs`
- `crates/hoi4-logic/src/economy/market_balance.rs`
- `crates/hoi4-logic/src/economy/building_tick_common.rs`
- `crates/hoi4-logic/src/economy/planned_tick.rs`
- `crates/hoi4-state/src/market.rs`
- `crates/hoi4-state/src/finance.rs`
- `crates/hoi4-ui/src/market_panel.rs`
- `crates/hoi4-app/src/main.rs`

问题：

- 建筑投入、POP 消费、政府采购、军工投入多数只写 demand，不真实扣 supply 或 stockpile。
- 同一份商品可能同时满足多个需求。
- 价格、满意度、库存覆盖天数都基于未清算账面数据。

修复方向：

- 新增统一商品清算结构，例如每日 `商品清算表`。
- 每种商品按需求桶分配：建筑投入、POP 基础消费、POP 非基础消费、政府采购、军工投入、建造投入、出口。
- 清算结果必须写出实际满足量、未满足量、期末库存、短缺比例。
- `market_balance.rs` 不再只做 surplus/deficit 补丁，而成为清算核心或被新清算核心替代。
- UI 市场面板展示清算结果，不再在 `main.rs` 中重新估算核心供需。

验收标准：

- 商品守恒测试通过：期初库存 + 生产 + 进口 - 消费 - 投入 - 出口 = 期末库存。
- POP 消费会扣库存。
- 建筑投入会扣库存或当日供给。
- 政府采购会占用商品。
- 军工生产会消耗投入品。

### P0.5：修复政府采购与军工生产假闭环 ✅ 已完成

关联文件：

- `crates/hoi4-state/src/finance.rs`
- `crates/hoi4-logic/src/economy/market_tick.rs`
- `crates/hoi4-logic/src/economy/planned_tick.rs`
- `crates/hoi4-logic/src/economy/stockpile.rs`
- `crates/hoi4-ui/src/finance_panel.rs`
- `crates/hoi4-ui/src/logistics_panel.rs`

问题：

- `gov_buy_for` 与 `gov_buy_on_credit_for` 扣钱但不真实扣商品。
- 市场经济军工生产只增加装备库存，不真实消耗投入商品。
- 计划经济和市场经济消耗口径不一致。

修复方向：

- 政府采购改为创建采购需求，由统一清算层结算实际成交。
- 成交后再确认财政支出、商品扣减和预算分项。
- 军工生产根据实际投入满足率计算产出。
- 市场经济和计划经济统一使用同一套投入消耗接口。

验收标准：

- 采购 100 单位钢后，市场钢可用量减少。
- 钢不足时坦克/火炮产出降低。
- 同一投入品不能被政府采购和军工生产重复使用。

### P0.6：修复 POP 消费、税后收入和失业人口需求 ✅ 已完成

关联文件：

- `crates/hoi4-state/src/pops.rs`
- `crates/hoi4-logic/src/economy/market_tick.rs`
- `crates/hoi4-logic/src/economy/planned_tick.rs`
- `crates/hoi4-logic/src/economy/finance_tick.rs`
- `crates/hoi4-ui/src/pop_panel.rs`
- `crates/hoi4-ui/src/finance_panel.rs`

问题：

- POP 没有真实可支配收入。
- 税收不扣 POP 钱，只写税负。
- 失业人口不产生消费需求。
- 消费需求不受价格和收入约束。

修复方向：

- 为 POP group 或阶级聚合增加收入、税额、可支配收入、基础消费预算。
- 工资先进入 POP 收入，再由财政收税。
- 所有人口都产生基础消费需求，失业人口可降低非基础消费。
- 消费满足度读取市场清算实际满足量。

验收标准：

- ✅ 失业人口仍消耗粮食和衣物。
- ✅ 提高税率会降低可支配收入和非基础消费。
- ✅ 粮食短缺会降低基础满足度并提高激进度。

### P0.7：分离财政经营收入与融资，重构 MEFO ✅ 已完成

关联文件：

- `crates/hoi4-state/src/finance.rs`
- `crates/hoi4-logic/src/economy/finance_tick.rs`
- `crates/hoi4-logic/src/economy/market_tick.rs`
- `crates/hoi4-content/src/v6_loader.rs`
- `crates/hoi4-ui/src/finance_panel.rs`
- `crates/hoi4-ui/src/topbar.rs`

问题：

- MEFO 通过 `receive` 增加 `daily_income_rm`。
- 财政面板会把融资覆盖看成收入。
- MEFO 缺少专项再武装预算和军工订单资金来源。

修复方向：

- 新增经营性收入、经营性支出、融资现金流、原始赤字、融资后现金变化字段。
- MEFO 进入融资现金流，不进入经营性收入。
- 增加 MEFO 专项军工预算或 `FundingSource::Mefo`。
- MEFO 利息、展期、危机、强制兑付在财政面板独立显示。

验收标准：

- MEFO 覆盖赤字后，经营性收入不变。
- 财政面板显示原始赤字与 MEFO 覆盖额。
- MEFO 军工订单增加隐性债务并提高军工采购。

### P0.8：研究经费真实扣款 ✅ 已完成

关联文件：

- `crates/hoi4-logic/src/research.rs`
- `crates/hoi4-logic/src/economy/finance_tick.rs`
- `crates/hoi4-state/src/finance.rs`
- `crates/hoi4-ui/src/research.rs`
- `crates/hoi4-content/src/eval.rs`

问题：

- 研究只检查现金，不扣研究经费。
- `AddTechBonus` content effect 没有真实接入研究状态。

修复方向：

- 每个研究槽每日从 treasury 扣研究支出。
- 现金不足时按实际支付比例降低研究速度。
- 研究支出进入预算分项。
- 科研 bonus 写入研究状态或国家科研 bonus 池。

验收标准：

- 研究推进一天后 treasury cash 减少。
- 现金不足时研究速度降低或暂停。
- focus 添加的科研 bonus 能被对应研究消费。

### P0.9：建造资金、材料和 CP 三重约束 ✅ 已完成

关联文件：

- `crates/hoi4-logic/src/economy/construction_tick.rs`
- `crates/hoi4-logic/src/economy/construction_planner.rs`
- `crates/hoi4-logic/src/economy/mod.rs`
- `crates/hoi4-logic/src/economy/finance_tick.rs`
- `crates/hoi4-state/src/buildings_v6.rs`
- `crates/hoi4-ui/src/construction_v6_panel.rs`

问题：

- 建造进度主要由 CP 推进。
- 政府建设不需要真实项目资金。
- 私人投资扣固定门槛，与实际项目成本脱节。
- 材料短缺不真实限制项目进度。

修复方向：

- 每个建设项目保存预算需求、已支付资金、材料需求、已消耗材料、CP 进度。
- 政府项目每日占用财政预算或融资。
- 私人项目按真实项目成本消耗投资账户。
- 材料由市场清算层提供，短缺降低有效 CP。
- 取消项目按规则退款或转为沉没成本。

验收标准：

- ✅ 政府没钱时建设降速或停止。
- ✅ 私人投资项目消耗投资池余额并按成本推进。
- ✅ 钢/机械短缺时建筑进度下降。

### P0.10：修复贸易总容量与封锁位置 ✅ 已完成

关联文件：

- `crates/hoi4-logic/src/trade/mod.rs`
- `crates/hoi4-state/src/trade.rs`
- `crates/hoi4-state/src/market.rs`
- `crates/hoi4-logic/src/naval/missions.rs`
- `crates/hoi4-logic/src/naval/sea_control.rs`
- `crates/hoi4-ui/src/trade_panel.rs`
- `crates/hoi4-ui/src/market_panel.rs`

问题：

- 港口/铁路容量按每个商品重复使用。
- 封锁只看敌方舰队总数，不看港口、航线、海区。
- world spot market 不是全球扣减池。

修复方向：

- 国家每日贸易 capacity 改为总量池。
- 多商品进口按优先级分配 capacity。
- world spot market 改为全局日内扣减。
- 封锁必须绑定航线海区、敌方舰队位置、制海权和港口。
- 修复 exporter exports 双计。

验收标准：

- ✅ 港口容量 100 时，多商品进口总量不能超过 100。
- ✅ 敌舰不在航线海区不能封锁该贸易。
- ✅ 多国进口同一 world spot 商品会共同消耗全局供应。

### P0.11：修复中日战争军阀自动参战 ✅ 已完成

关联文件：

- `crates/hoi4-logic/src/diplomacy/war.rs`
- `crates/hoi4-runtime/src/schedule.rs`
- `crates/hoi4-state/src/diplomacy.rs`
- `crates/hoi4-content/src/situation.rs`
- `crates/hoi4-content/src/event_tick.rs`
- `crates/hoi4-ai/src/china_theater.rs`

问题：

- `reconcile_war_membership` 每日把 primary attacker/defender faction 全员补进战争。
- 中国军阀加入统一战线后可能无条件自动参战。

修复方向：

- 增加战争成员自动加入策略，例如 `自动参战`、`延迟参战`、`禁止自动参战`。
- faction membership 不等于 war membership。
- 中日战争 situation 明确控制哪些势力立即参战、哪些延迟参战。
- 对地方军增加战区限制或地方自治逻辑。

验收标准：

- ✅ 1937 卢沟桥后，指定军阀在统一战线但不在战争中。
- ✅ 每日 reconcile 后延迟参战状态不被破坏。
- ✅ 指定事件触发后，地方军才加入战争。

### P0.12：建立日本对华大陆增援与登陆计划 ✅ 已完成

关联文件：

- `crates/hoi4-ai/src/ground_orders.rs`
- `crates/hoi4-ai/src/ground.rs`
- `crates/hoi4-ai/src/naval.rs`
- `crates/hoi4-ai/src/china_theater.rs`
- `crates/hoi4-ai/src/orchestrator.rs`
- `crates/hoi4-logic/src/military/movement.rs`
- `crates/hoi4-logic/src/naval/missions.rs`
- `crates/hoi4-logic/src/naval/movement.rs`
- `crates/hoi4-state/src/frontline.rs`

问题：

- 日本海外增援用粗略港口选择，不懂华北、上海、青岛等战区目标。
- 登陆 API 存在，但陆军和海军没有共享的登陆计划。
- 海军支援在调度上可能晚于陆军登陆尝试。

修复方向：

- 新增对华战区增援计划，明确本土、朝鲜、满洲、华北、上海方向。
- 新增 `NavalInvasionPlan` 或同等状态机。
- 登陆计划包含目标省、出发港、路线海区、 assigned divisions、assigned fleets、准备时间、支援阈值、状态。
- 海军 AI 先为计划分配 route support，陆军满足条件后执行登陆。
- 日本战力评估区分本土师、大陆师、运输中师、登陆准备师。

验收标准：

- ✅ 1937 后日本本土师会向大陆友港或登陆准备点移动。
- ✅ 日本能生成上海、青岛或天津登陆计划。
- ✅ 没有制海权时计划等待，有制海权时执行。

### P0.13：统一师命令执行权，避免多系统覆盖 destination ✅ 已完成

关联文件：

- `crates/hoi4-ai/src/frontline_ai.rs`
- `crates/hoi4-ai/src/ground_orders.rs`
- `crates/hoi4-logic/src/military/frontline.rs`
- `crates/hoi4-logic/src/military/movement.rs`
- `crates/hoi4-state/src/frontline.rs`
- `crates/hoi4-ui/src/military.rs`

问题：

- frontline AI、ground orders、frontline executor、movement API 都可能写 division destination。
- 高优先级命令可能被低优先级系统覆盖。

修复方向：

- 新增统一命令执行层。
- AI 只输出意图：守备、进攻、运输、登陆、清剿、撤退。
- 每个师保存当前命令来源、优先级、过期时间和目标。
- 只有统一 executor 写 destination。

验收标准：

- ✅ 登陆命令不会被普通前线推进覆盖。
- ✅ 手动命令优先级高于 AI 命令。
- ✅ 同一师同一天只由一个执行层写最终目的地。

### P1.1：战争投降与和平自动结算 ✅ 已完成

关联文件：

- `crates/hoi4-logic/src/diplomacy/surrender.rs`
- `crates/hoi4-logic/src/diplomacy/peace.rs`
- `crates/hoi4-logic/src/diplomacy/war.rs`
- `crates/hoi4-runtime/src/schedule.rs`
- `crates/hoi4-content/src/surrender.rs`
- `crates/hoi4-ui/src/diplomacy.rs`

问题：

- peace conference 有函数但缺少 daily 自动入口。
- 投降移出战争后可能留下空战争或旧 at_war 状态。

修复方向：

- 增加 `tick_peace_resolution_daily`。 ✅
- 投降和和平共用战争清理函数。 ✅
- 删除空战争，重算所有相关国家 at_war。 ✅
- scripted surrender 与普通战争结算统一出口。 ✅

验收标准：

- ✅ 战争一方全部投降后，战争删除或进入和平会议。
- ✅ 所有参战国 at_war 状态正确。
- ✅ 省份转移、吞并、傀儡化结果可测试。

### P1.2：修复事件 cascade 作用域和顺序 ✅ 已完成

关联文件：

- `crates/hoi4-runtime/src/content_runtime.rs`
- `crates/hoi4-content/src/event_tick.rs`
- `crates/hoi4-logic/src/diplomacy/surrender.rs`
- `crates/hoi4-app/src/update_loop.rs`

问题：

- pending trigger 只保存 event id，不保存作用国家。
- cascade 可能使用玩家国家作为作用域。
- Vec pop 导致 LIFO 顺序。

修复方向：

- pending trigger 改为结构体：事件 id、作用国家、显示国家、来源。
- 使用 FIFO 队列。
- 新闻事件和国家事件分开处理。
- UI 暂停判断检查是否存在任意国家事件。

验收标准：

- ✅ 法国投降事件作用于法国，而不是玩家国家。
- ✅ A 触发 B、C 时顺序为 B 再 C。
- ✅ 国家事件出现时自动暂停。

### P1.3：content effect 错误报告化 ✅ 已完成

关联文件：

- `crates/hoi4-content/src/eval.rs`
- `crates/hoi4-content/src/event_tick.rs`
- `crates/hoi4-content/src/decision_tick.rs`
- `crates/hoi4-runtime/src/content_runtime.rs`
- `crates/hoi4-app/src/main.rs`
- `crates/hoi4-logic/src/feedback.rs`

问题：

- 多处 effect 调用使用 `let _ = ...` 静默吞错。
- 法律、外交、事件效果失败时 UI 不知道。

修复方向：

- effect runner 返回 `效果报告`，包含 applied、warnings、errors。
- content tick 汇总错误并写入反馈总线或 UI 通知。
- UI 命令失败应显示中文提示。

验收标准：

- ✅ invalid tag effect 产生错误报告。
- ✅ PP 不足切法律时 UI 或日志显示失败原因。

### P1.4：统一建筑利润、GDP 和商品 RM 口径 ✅ 已完成

关联文件：

- `crates/hoi4-logic/src/economy/finance_tick.rs`
- `crates/hoi4-logic/src/economy/building_runtime.rs`
- `crates/hoi4-logic/src/economy/market_tick.rs`
- `crates/hoi4-app/src/main.rs`
- `crates/hoi4-ui/src/finance_panel.rs`
- `crates/hoi4-ui/src/construction_v6_panel.rs`

问题：

- GDP scale、GOODS_RM_SCALE、UI fallback GDP 口径不统一。
- 财政税收、建筑利润、工资可付性、GDP 可能使用不同估算。
- 建造面板用 actual profit 和 expected profit 取最大值，可能掩盖亏损。

修复方向：

- 新增统一 valuation helper。
- 建筑 tick 计算一次 output value、input cost、wage bill、profit。
- 财政、GDP、UI 均读取同一 runtime 字段。
- UI 分开显示实际利润和估算利润。

验收标准：

- ✅ 同一建筑在市场、财政、建造面板利润一致。
- ✅ 亏损建筑不会被估算利润掩盖。
- ✅ UI GDP 与 treasury GDP 口径一致。

### P1.5：计划经济路径修复 ✅ 已完成

关联文件：

- `crates/hoi4-logic/src/economy/planned_tick.rs`
- `crates/hoi4-logic/src/economy/market_tick.rs`
- `crates/hoi4-state/src/buildings_v6.rs`
- `crates/hoi4-state/src/laws.rs`

问题：

- 计划经济只处理国有建筑，私有建筑可能停摆。
- 配给后再用剩余供给计算满意度，可能双重惩罚。

修复方向：

- 经济法切到计划经济时明确私有建筑处理：国有化、保留私营但纳入计划、或逐步转制。
- 计划经济也使用统一商品清算层。
- 配给满足度来自实际发放量，而不是发放后的剩余供给。

验收标准：

- ✅ 切计划经济后私有建筑不会无声停摆。
- ✅ 库存刚好满足配给需求时，满意度不被二次惩罚。

### P1.6：UI 数据权威源与面板缓存 ✅ 已完成

关联文件：

- `crates/hoi4-app/src/main.rs`
- `crates/hoi4-app/src/ui_binding/frame.rs`
- `crates/hoi4-app/src/ui_binding/topbar.rs`
- `crates/hoi4-ui/src/finance_panel.rs`
- `crates/hoi4-ui/src/market_panel.rs`
- `crates/hoi4-ui/src/construction_v6_panel.rs`
- `crates/hoi4-ui/src/diplomacy.rs`
- `crates/hoi4-ui/src/military.rs`

问题：

- UI 在 render 路径大量重算经济、市场、外交、军事数据。
- 市场面板、建造面板、财政面板可能使用不同估算。

修复方向：

- 新增 `UiPanelCache` 或同等缓存层。
- 经济、市场、外交、军事、POP、建筑各自有 revision。
- UI 只展示逻辑 summary，不在面板中重新实现规则。
- 大列表虚拟化或分页。

验收标准：

- 打开市场面板时不重新推导核心供需。
- 外交面板只为选中国家构造详情。
- perf 日志显示 panel build 耗时可观测。

### P1.7：前线渲染与箭头性能修复 ✅ 已完成

关联文件：

- `crates/hoi4-app/src/render_collect.rs`
- `crates/hoi4-app/src/main.rs`
- `crates/hoi4-app/src/passes/maparrow.rs`
- `crates/hoi4-logic/src/military/frontline.rs`
- `crates/hoi4-state/src/frontline.rs`

问题：

- 前线箭头重建可能扫描整张 province_map。
- 静态 frontlines buffer 可能不随战争和占领变化更新。

修复方向：

- 预计算 province pair shared border 或 province boundary segments。
- 箭头重建只处理前线路径相关省份。
- 战争创建、占领变化、和平、投降后标记前线 overlay dirty。
- 如果旧静态 overlay 与动态箭头重复，考虑删除旧 overlay。

验收标准：

- 拖动前线或选择师时 arrows 不出现 60ms 级卡顿。
- 战争开始后前线 overlay 自动出现。
- 占领变化后前线显示刷新。

### P2.1：运行时索引和性能缓存

关联文件：

- `crates/hoi4-state/src/world.rs`
- `crates/hoi4-state/src/store.rs`
- `crates/hoi4-state/src/pops.rs`
- `crates/hoi4-state/src/buildings_v6.rs`
- `crates/hoi4-logic/src/economy/market_tick.rs`
- `crates/hoi4-logic/src/economy/finance_tick.rs`
- `crates/hoi4-logic/src/trade/mod.rs`

问题：

- 多处每国每日扫描全局 POP、建筑、国家、商品。
- 贸易撮合近似国家乘商品乘国家。
- POP 就业拆分会导致 group 碎片化。

修复方向：

- 建立国家到州、POP group、建筑、舰队、军队的索引。
- 建立商品、生产方法、装备类别 HashMap 索引。
- 定期合并相同州、阶级、职业、雇主和属性接近的 POP group。
- 贸易出口 surplus 建索引。

验收标准：

- 365 天回放 POP group 数不无限增长。
- 经济 tick 不再按国家重复扫描全局建筑和 POP。
- 贸易撮合耗时随国家数增长更平滑。

### P2.2：测试体系补全

关联文件：

- `crates/hoi4-logic/tests/`
- `crates/hoi4-ai/tests/`
- `crates/hoi4-content/tests/`
- `crates/hoi4-integration/tests/`
- `crates/hoi4-ui/tests/`

问题：

- 现有测试多验证函数存在或不崩，不验证闭环和长期行为。
- GUI/headless 一致性、商品守恒、历史节奏、AI 战役行为覆盖不足。

修复方向：

- 新增守恒测试、历史日期测试、长期回放测试、UI 数据一致性测试、AI 行为测试、性能预算测试。
- 每个 P0 修复必须有至少一个防回归测试。
- 测试说明和断言消息使用中文。

验收标准：

- P0 任务全部有测试覆盖。
- 365 天全球回放稳定且无 NaN、无库存爆炸。
- GUI/headless 内容触发结果一致。

## 阶段一：统一运行时与内容调度 ✅ 已完成

### 问题

当前 `hoi4-runtime::tick_one_hour` 负责小时调度，但 content daily、focus、decision、situation、scripted surrender 等实际在 app 层额外调用。GUI 与 headless 路径不一致，高速或卡帧时还可能跳过中间日期。

### 重构目标

把 content runtime 纳入统一 runtime，让 GUI、headless、测试都走同一条日更路径。

### 任务

- ✅ 新增统一的内容运行状态 `ContentRuntimeState`，包含 focus、event、decision、situation、global flags、scripted surrender 与 last content day。
- ✅ 将 content daily 从 app update loop 移入 runtime day-change 流程（`SystemSchedule` Content daily 系统）。
- ✅ 如果一帧推进多天，必须逐日补跑 content daily（由 `SystemSchedule` daily cadence 保证）。
- ✅ headless 必须加载同一套内容状态（`bootstrap.rs` 构造同一 `ContentRuntimeState`）。
- situation effects 应在 runtime 或统一 content executor 中处理，不应只存在 GUI 路径。
- content tick 应返回详细报告：触发事件、完成 focus、完成 decision、surrender 结果、effect 错误。

### 验收测试

- ✅ 高速推进 72 小时，content daily 执行 3 次。
- ✅ headless 跑到固定历史日期，事件、focus、situation 与 GUI 路径一致。
- 1937 年卢沟桥事件 flag 能在 headless 与 GUI 中同时触发。
- scripted surrender 在 headless 中也能触发并处理。

## 阶段二：法律与 focus 权威源统一

### 问题

法律 pending/cooldown 有设置入口，但没有挂入 daily schedule。focus 同时存在 politics daily 与 RON content daily 两套推进逻辑，共享同一组状态字段，可能双推进或丢失完成效果。

### 重构目标

法律与 focus 必须各自只有一个权威运行时。

### 任务

- 将 `tick_law_cooldowns` 挂入 daily schedule。
- 明确法律生效顺序：法律冷却推进、法律生效、副作用执行、政治 modifier 重算、经济 tick。
- 统一 focus 运行时，选择 RON focus 或 vanilla focus 作为唯一权威。
- 如果 RON focus 是权威，应从 politics daily 中移除 focus progress 推进。
- focus `available` 条件必须接入 UI 与 start 逻辑。
- focus 完成效果只允许一个 effect runner 执行。
- 科研 bonus、工业 bonus、战争目标等 focus effect 必须写入真实系统状态。

### 验收测试

- 法律切换扣 PP 后，推进冷却天数，法律生效且 pending 清空。
- 不满足 `available` 的 focus 在 UI 中锁定，且 start 调用失败。
- 一个 70 天 focus 推进 70 天只完成一次，只执行一次效果。
- focus 添加科研加成后，对应研究实际获得加速或 ahead penalty 减免。

## 阶段三：经济实物清算核心

### 问题

当前市场供需主要是账面统计，建筑投入、POP 消费、政府采购、军工生产多数不真实扣减 supply 或 stockpile。

### 重构目标

建立统一市场清算层，使所有商品流动满足守恒。

### 新的市场清算模型

每日每国每种商品应形成以下流程：

1. 期初库存。
2. 国内生产。
3. 进口。
4. 可用供给。
5. 需求桶分配。
6. 实际满足量。
7. 未满足量。
8. 出口。
9. 期末库存。

需求桶至少包括：

- 建筑投入。
- POP 基础消费。
- POP 非基础消费。
- 政府采购。
- 军工生产投入。
- 建造投入。
- 出口订单。

### 任务

- 新增统一商品清算函数，所有系统通过它申请商品。
- 建筑生产根据实际投入满足率产出。
- POP 消费根据实际满足量计算满意度。
- 政府采购必须真实扣商品或创建待清算订单。
- 军工生产必须真实消耗钢、机械、燃料等投入。
- 价格系统读取清算后的短缺、库存覆盖天数和未满足需求。
- UI 市场面板展示清算结果，不再自己估算核心需求。

### 验收测试

- 商品守恒：期初库存 + 生产 + 进口 - 消费 - 投入 - 出口 = 期末库存。
- 政府采购后市场可用商品减少。
- 军工生产装备时投入商品减少。
- POP 消费后消费品库存减少。
- 同一商品不能被多个需求桶重复使用。

## 阶段四：POP、工资、消费与满意度闭环 ✅ 部分完成

### 问题

当前 POP 没有真实钱包，税收不扣 POP 可支配收入，失业人口不产生消费需求，消费不受价格和收入约束。

### 重构目标

建立至少按 POP group 或阶级聚合的收入、税后收入、消费预算和满意度闭环。

### 任务

- 为 POP 或阶级聚合增加可支配收入字段。
- 工资先进入 POP 收入，再扣税。
- 消费需求根据人口、阶级、价格、可支配收入与需求层级生成。
- 失业人口必须产生基础消费需求。
- 消费满足度来自实际清算结果。
- 税负影响满意度，但不应替代真实收入扣减。
- 阶级流动应使用真实岗位需求，而不是当前就业人数。

### 验收测试

- 失业人口仍消耗粮食和衣物。
- 提高税率会减少 POP 可支配收入与消费能力。
- 粮食短缺降低基础需求满足度并提高激进度。
- 新建工厂产生岗位需求后，农民到工人的流动加快。

## 阶段五：财政、MEFO、研究与建造资金闭环

### 问题

MEFO 当前被当作收入覆盖赤字。研究只检查现金，不扣钱。建造项目主要消耗 CP，不真实消耗项目资金。德国财政收入与支出模型口径不对称。

### 重构目标

分离经营性收入、经营性支出、融资现金流与债务，建立真实预算压力。

### 财政分类

财政面板和逻辑层应区分：

- 税收收入。
- 企业利润收入。
- 关税收入。
- 国有企业收入。
- 行政支出。
- 福利支出。
- 军费支出。
- 军工采购支出。
- 建造支出。
- 研究支出。
- 债务利息。
- MEFO 融资。
- 公债融资。
- 原始赤字。
- 融资后现金变化。

### MEFO 重构

- MEFO 不得计入 `daily_income_rm`。
- MEFO 应计入融资现金流或隐性债务融资。
- 增加 MEFO 专项再武装预算。
- 军工订单应可以指定资金来源为 MEFO。
- MEFO 利息、展期、危机、强制兑付都应显示在财政面板。

### 研究重构

- 每个研究槽每日扣研究经费。
- 现金不足时按比例降低研究速度。
- 研究支出进入预算分项。
- 科研 bonus 应进入研究系统并被消费。

### 建造重构

- 政府建设项目必须占用预算或债务。
- 私人建设项目必须真实使用投资池资金。
- 建造取消、失败、切换应处理退款或沉没成本。
- 建造速度受 CP、资金、材料三者共同限制。

### 验收测试

- MEFO 覆盖赤字后，经营性收入不变，融资项增加。
- 研究每日扣现金并显示预算支出。
- 现金不足导致研究降速。
- 政府建设无预算时无法全速推进。
- 私人投资项目取消后按规则退款或计入沉没成本。
- 德国 1936 开局财政收入、支出、军费、MEFO 赤字在历史区间内。

## 阶段六：贸易、封锁与全球市场重构

### 问题

贸易容量按每种商品重复使用。封锁只看敌方舰队总数，不看海区。世界现货市场不是全局稀缺池。

### 重构目标

贸易必须受总运输能力、航线、海军控制和全球供给约束。

### 任务

- 港口、铁路、船运 capacity 改为国家每日总容量。
- 多商品进口按优先级、价格、协议与战略权重分配容量。
- world spot market 改为全球日内扣减池。
- 出口统计修复双计。
- 封锁绑定海区、航线、港口和制海权。
- 封锁影响不只 oil/rubber，也应影响粮食、煤、钢、民生商品等。

### 验收测试

- 港口容量 100 时，所有商品进口总和不能超过 100。
- 多个国家进口同一 world spot 商品会共同消耗全局供给。
- 敌方舰队不在航线海区时不能封锁港口。
- 封锁粮食进口会降低民生满足度。

## 阶段七：军事 AI、登陆与中日战争修复

### 问题

日本跨海调兵使用粗略港口选择，登陆缺少计划状态机。统一战线可能把军阀自动拉入战争。前线与 ground orders 多系统竞争写师目的地。

### 重构目标

把军事 AI 从零散命令改为战区计划，再由统一执行层写真实命令。

### 任务

- ✅ 修复 `reconcile_war_membership`，阵营成员不应无条件自动参战。
- ✅ 为中日战争增加延迟参战、地方军自治、战区范围限制。
- ✅ 日本对华战争增加大陆增援计划。
- ✅ 日本对华战争增加上海、青岛、天津等登陆计划。
- ✅ 新增 `登陆计划` 状态机：准备、等待制海、出航、登陆、扩大战果、失败恢复。
- ✅ 海军 AI 根据登陆计划分配舰队到 route sea regions。
- ✅ 陆军 AI 只在制海和准备达标后执行登陆。
- ✅ 战力评估改为战区局部，不把本土师算作大陆前线可用战力。
- ✅ division destination 改为统一执行层写入，AI 只输出意图。

### 验收测试

- ✅ 1937 卢沟桥后，指定军阀加入统一战线但不自动参战。
- ✅ 日本 180 天内将一定数量本土师调往大陆。
- ✅ 日本能生成上海或青岛登陆计划。
- ✅ 登陆计划没有制海权时等待，有制海权后执行。
- 中日战争 180 天前线有合理推进，但不会迅速灭亡中国。
- ✅ 多系统不会互相覆盖同一师的高优先级命令。

## 阶段八：战争、投降、和平与事件作用域

### 问题

peace conference 有函数，但缺少自动结算入口。surrender cascade 可能用玩家国家作为事件作用域。投降移出战争后可能残留空战争或旧 at_war 状态。

### 重构目标

战争必须能自然开始、推进、投降、结算与清理。

### 任务

- 增加每日和平结算入口。 ✅
- 投降后删除空战争，并重算所有相关国家 at_war。 ✅
- scripted surrender 与普通 peace resolution 统一出口。 ✅
- pending trigger 必须保存事件 id、作用国家、显示国家、触发来源。 ✅
- 事件 cascade 改为 FIFO。 ✅
- UI 暂停判断应检查是否存在任意国家事件，而不是只看队尾。 ✅
- content effect runner 返回错误报告，不再静默吞错。 ✅

### 验收测试

- 两国战争中一方投降后，战争删除，双方 at_war 为 false。
- 投降新闻作用于投降国，而不是玩家国家。 ✅
- 连锁事件 A 触发 B、C，顺序为 B 再 C。 ✅
- invalid tag effect 产生错误报告。

## 阶段九：UI 数据权威源与性能优化

### 问题

UI 面板在渲染路径中大量重算经济、外交、市场、军事数据。部分面板用估算代替真实逻辑字段，可能误导玩家。

### 重构目标

UI 只展示逻辑层 summary 或缓存快照，不重新实现游戏规则。

### 任务

- 新增 UI 面板缓存层。
- 每类面板绑定 revision：经济、市场、外交、军事、POP、建筑、贸易。
- Finance、Market、Construction 共享同一套经济 summary。
- 建造利润拆分实际利润与估算利润。
- CP 显示拆成总建造力、已分配、闲置、材料限制后有效建造力。
- 外交面板列表轻量化，选中国家时才构造详情。
- 大列表使用虚拟化或分页。
- 市场面板不再用 UI heuristic 估算政府采购和 POP 需求。
- 前线箭头不再全图扫描 province_map。
- 静态前线 overlay 改为 dirty rebuild 或删除。

### 验收测试

- UI 市场需求与逻辑层清算结果一致。
- 亏损建筑不会因为估算利润为正而显示盈利。
- 战争创建后前线 overlay 自动刷新。
- 拖动前线时 arrows 重建不出现 60ms 级卡顿。
- 外交面板打开时不会每帧构造所有国家完整详情。

## 阶段十：性能索引与长期回放

### 问题

经济、贸易、前线、UI 多处存在全局扫描。小时分摊只能降低尖峰，不能降低总复杂度。

### 重构目标

建立长期运行所需的索引、缓存和回放测试。

### 任务

- 建立国家到州、POP group、建筑、舰队、军队的运行时索引。
- 建立商品、生产方法、装备类别的 HashMap 索引。
- POP group 定期合并，防止就业拆分导致碎片化。
- 贸易出口国 surplus 建索引，避免国家乘商品乘国家扫描。
- 前线边界预计算 province pair shared border。
- perf 日志增加面板构建耗时、箭头重建原因、清算耗时、国家经济 tick 耗时。

### 验收测试

- 365 天全球回放无 NaN、无极端库存爆炸、无 POP group 无限增长。
- 商品守恒测试覆盖 30 天、180 天、365 天。
- 5 速下日均模拟耗时在预算内。
- 打开外交、市场、建造、军事面板时帧耗时可控。

## 测试体系重建

### 必须新增的测试类别

1. 守恒测试。
2. 历史日期测试。
3. GUI/headless 一致性测试。
4. 长期回放测试。
5. UI 数据一致性测试。
6. AI 行为测试。
7. 性能预算测试。

### 建议测试清单

- 商品清算守恒测试。
- POP 消费扣库存测试。
- 政府采购扣实物测试。
- 军工生产消耗投入测试。
- 研究每日扣钱测试。
- 建造资金来源测试。
- MEFO 不计入经营性收入测试。
- 德国 1936 财政区间测试。
- 港口总容量约束测试。
- 封锁海区位置测试。
- 法律冷却生效测试。
- focus available 条件测试。
- focus 不双推进测试。
- headless content 日期触发测试 ✅。
- 高速推进不跳日测试 ✅。
- 1937 中日战争军阀延迟参战测试 ✅。
- 日本大陆增援测试 ✅。
- 日本登陆计划测试 ✅。
- 投降清理战争测试 ✅。
- 事件作用域测试 ✅。
- UI 经济字段一致性测试。
- 登陆命令不被前线覆盖测试 ✅。
- 手动命令优先级高于 AI 命令测试 ✅。
- 同一师同日单目的地测试 ✅。
- 前线箭头性能测试。

## 重构执行顺序总表

| 阶段 | 名称 | 优先级 | 主要收益 |
|---|---|---|---|
| 1 | 统一运行时与内容调度 ✅ | 最高 | 修复 GUI/headless 不一致、历史事件跳日 |
| 2 | 法律与 focus 权威源统一 | 最高 | 修复法律不生效、focus 双推进 |
| 3 | 经济实物清算核心 | 最高 | 修复市场假闭环 |
| 4 | POP、工资、消费与满意度闭环 | 高 | 修复人口经济失真 |
| 5 | 财政、MEFO、研究与建造资金闭环 | 高 | 修复德国财政、MEFO、研究、建造失真 |
| 6 | 贸易、封锁与全球市场重构 | 高 | 修复贸易容量、封锁、全球市场失真 |
| 7 | 军事 AI、登陆与中日战争修复 | 高 | 修复日本不登陆、不增援、军阀全入战 |
| 8 | 战争、投降、和平与事件作用域 | 高 | 修复战争不能自然结束、事件改错国家 |
| 9 | UI 数据权威源与性能优化 | 中高 | 修复面板误导和每帧重算 |
| 10 | 性能索引与长期回放 | 中高 | 支撑全球长期运行 |

## 中文内容要求

后续所有新增或修改内容应遵守以下规则：

- 新增文档必须使用中文。
- 新增路线图、审计报告、测试说明必须使用中文。
- 新增 UI 文案必须使用中文。
- 新增日志说明优先使用中文，除非该日志用于机器解析或已有英文格式。
- 新增测试函数名可使用英文以符合 Rust 习惯，但测试注释、断言消息、测试文件说明应使用中文。
- 新增代码注释应使用中文，除非所在文件已明显采用英文注释风格且混用会降低一致性。
- 不要求重命名已有英文函数、结构体、字段或 crate。
- 不要求翻译外部协议、Rust 标准术语、库 API 名称。

## 最终目标状态

完成本路线图后，项目应达到以下状态：

- 1936 开局主要国家财政、GDP、军费、贸易、人口消费处于合理区间。
- 德国 MEFO 是可见的隐性融资和再武装预算，而不是收入伪装。
- 日本在 1937 后能按战区计划增援大陆并组织沿海登陆。
- 中国军阀不会因为加入统一战线被无条件自动拉入战争。
- 市场供需、库存、价格、满意度、建筑投入、军工生产满足基本守恒。
- focus、event、decision、situation 在 GUI 与 headless 中一致运行。
- 战争能自然投降、清理、和平结算。
- UI 显示的是逻辑层权威快照，不再用面板私有估算误导玩家。
- 365 天全球回放稳定，性能和数值均可观察、可测试、可回归。
